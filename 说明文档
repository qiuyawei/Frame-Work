create time : 2021.01.14 pm 15:23
目的：自己封装一个项目架构，方便快速开始项目
架构模式：app(入口) <---(业务模块：例如 登录、个人中心等等) <----(base lib 基础模块，常用的库，工具类，自定义view等等)

# 2021.1.18
备忘录：aRouter库的调试，还未完成。

#2021.1.20
aRouter库的调试完成，其中有些地方需要注意：
1.每个模块都需要集成这个库，单独base模块依赖无法跳转，报错找不到
2.kotln 依赖不同于 java依赖
3.这个库有许多好用的功能，例如：拦截器，外部通过uri指定跳转到某个页面等，具体参照作者文档。

#2021.1.21
基本配置已经完成，这将作为下次项目的基本框架使用

